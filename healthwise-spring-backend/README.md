# HealthWise Spring Backend
Spring Boot 3 / Java 21 persistence API. It owns authentication, JWT refresh tokens, user profiles, and MongoDB CRUD only. It contains no AI, Gemini, LangGraph, Docling, or clinical reasoning.

Set `MONGODB_URI` and a 32+ character `JWT_SECRET`, then run `mvn spring-boot:run`.
